from distutils.core import setup

setup(
    name='remotipy',
    packages=['remotipy'],  # this must be the same as the name above
    version='0.1.2',
    description='Remote RPC over HTTP for python',
    author='Tiziano Piccardi',
    author_email='t.piccardi@unitn.it',
    url='https://github.com/tizianopiccardi/remotipy',  # use the URL to the github repo
    download_url='https://github.com/tizianopiccardi/remotipy/tarball/0.1',  # I'll explain this in a second
    keywords=['rcp', 'http', 'serialization'],  # arbitrary keywords
    classifiers=[],
)
